# Optional path to the SQLite database file. If not provided, defaults to backend/instance/notes.db
DATABASE_PATH=./instance/notes.db

# Enable seed on startup (true/false). If true, seeds with sample notes if DB is empty.
SEED_ON_STARTUP=true
